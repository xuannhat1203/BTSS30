/*
    ComponentDidMount:
        - Phương thức này được gọi sau khi component đã render và mount vào DOM.
        - Bạn có thể sử dụng nó để thực hiện các hành động tương tác với DOM sau khi component đã được render, như thay đổi tiêu đề của tab trình duyệt.
        - Trong phương thức này, bạn có thể truy cập và thao tác với DOM thông qua API của trình duyệt, ví dụ như document.title để thay đổi tiêu đề của tab.
 */